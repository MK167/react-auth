# Minimal image — only Express mock API (mock-server/server.cjs + db.json)
FROM node:22-bookworm-slim

WORKDIR /app/mock-server

COPY mock-server/package.json mock-server/package-lock.json ./
RUN npm ci --omit=dev

COPY mock-server/ ./

ENV MOCK_PORT=3001
EXPOSE 3001

CMD ["node", "server.cjs"]
